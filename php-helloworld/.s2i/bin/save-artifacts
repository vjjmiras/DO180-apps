#!/bin/bash

ARTIFACTS_DIR=/opt/app-root/src/vendor

if [ ! -d $ARTIFACTS_DIR ]; then
  mkdir $ARTIFACTS_DIR
fi

echo "Build timestamp: $(date)" >> ${ARTIFACTS_DIR}/timestamp

/usr/libexec/s2i/save-artifacts
